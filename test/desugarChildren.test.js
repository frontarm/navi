describe('desugarChildren', function() {
  it('returns a RouteSet as-is')
  it('finds branches in the passed in JunctionSet')
  it('fails if a route with an unknown branch is passed in')
  it('fails if multiple routes for the same branch are passed in')
})
