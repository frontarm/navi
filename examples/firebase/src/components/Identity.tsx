import React from 'react'
import styled from 'styled-components/macro'

interface IdentityProps {
  user: 
}

function Identity(props: IdentityProps) {

}

const StyledIdentity = styled(Identity)`

`

export default StyledIdentity