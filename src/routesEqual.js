export default function routesEqual(routeX, routeY) {
  // TODO:
  // - routeX / routeY can also be route sets
  // - use the branches set on each route to serialize params, and then recursively check children
  return false
}
